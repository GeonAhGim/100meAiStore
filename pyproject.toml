[project]
name = "smart-store-aios"
version = "0.1.0"
description = "Policy-first worker system for online consignment commerce"
requires-python = ">=3.10"

[project.scripts]
store-aios = "smart_store_aios.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

